# Gemfile

# plugin sources
source "https://rubygems.org"

# use latest jekyll version
gem "jekyll"

# plugins to load during build
# with proper site config
gem 'jekyll-admin', group: :jekyll_plugins
gem 'jekyll-scholar', group: :jekyll_plugins
